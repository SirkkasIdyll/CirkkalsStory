using System;
using Godot;

namespace CS.Scripts.Mobs;

public static partial class Actions
{
    /*public static void Attack(Mob attacker, Mob defender)
    {
        var attackRoll = attacker.Strength + Random.Shared.NextDouble() * attacker.Strength;
        GD.Print(attackRoll);
        defender.AlterHealth((int) -Math.Ceiling(attackRoll));
    }*/
}